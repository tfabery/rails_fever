<% if @user %>
  <%= button_to "New Post", new_user_post_path(@user), method: :get %>
<% end %>

<div class="row">
  <% @posts.each do |post| %>
    <%= render "tile", post: post %>
  <% end %>
</div>

<% if @user %>
  <%= button_to "New Post", new_user_post_path(@user), method: :get %>
<% end %>
